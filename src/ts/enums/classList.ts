const enum ClassList {
  navButton = 'nav__button',
  navButtonActive = 'nav__button_active',
  page = 'page',
  pageHide = 'page_hide',
  nav = 'nav',
  main = 'main',
  title = 'title',
  subtitle = 'subtitle',
}

export default ClassList;
